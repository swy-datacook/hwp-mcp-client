# 🔧 HWP MCP 연결 문제 해결 완료

## ❌ 문제점

Claude Desktop에서 HWP MCP 커넥터가 활성화되지 않는 문제 발생

**에러 메시지**:
```
Zod validation error: Invalid union, Expected string/number, received null
```

## 🔍 원인 분석

### 우리가 만든 방식 (잘못됨)
```
[Claude Desktop] → [HTTP 브릿지] → [HTTP 서버] → [stdio 서버]
```

### 올바른 방식 (참고: https://github.com/jkf87/hwp-mcp)
```
[Claude Desktop] → [stdio 서버] (직접 연결)
```

**핵심 차이점**:
- MCP 프로토콜은 **stdio (stdin/stdout) 직접 통신**을 사용
- HTTP 래퍼를 거치면 JSON-RPC 응답 형식이 깨짐
- Claude Desktop은 stdio로 직접 MCP 서버와 통신해야 함

## ✅ 해결 방법

### SSH를 통한 원격 stdio 서버 직접 실행

**설정 파일**: `~/Library/Application Support/Claude/claude_desktop_config.json`

```json
{
  "mcpServers": {
    "hwp-remote": {
      "command": "sshpass",
      "args": [
        "-p",
        "datacook",
        "ssh",
        "Data303@106.241.28.49",
        "C:\\hwp-mcp\\venv\\Scripts\\python.exe C:\\hwp-mcp\\src\\hwp_mcp_stdio_server.py 2>C:\\hwp-mcp\\logs\\stdio_stderr.log"
      ]
    }
  }
}
```

### 동작 방식

```
[Mac Claude Desktop]
    ↓ stdio (stdin/stdout)
[sshpass + SSH]
    ↓ SSH 터널
[Windows 서버: 106.241.28.49]
    ↓
[Python stdio 서버]
    ↓
[HWP COM 객체]
```

## 🧪 테스트 결과

```bash
echo '{"jsonrpc":"2.0","id":1,"method":"initialize",...}' | \
  sshpass -p 'datacook' ssh Data303@106.241.28.49 \
  "C:\\hwp-mcp\\venv\\Scripts\\python.exe C:\\hwp-mcp\\src\\hwp_mcp_stdio_server.py"

# 응답:
{"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2024-11-05",...}}
```

✅ **정상 작동 확인**

## 📚 참고한 저장소

**GitHub**: https://github.com/jkf87/hwp-mcp

**핵심 설정 방식**:
```json
{
  "mcpServers": {
    "hwp": {
      "command": "python",
      "args": ["경로/hwp-mcp/hwp_mcp_stdio_server.py"]
    }
  }
}
```

이 방식은 **로컬에서 Python 스크립트를 직접 실행**하는 것입니다.

우리는 원격 서버이므로 **SSH를 통해 원격 실행**하도록 수정했습니다.

## 🎯 최종 설정

### 1. Claude Desktop 설정 완료
- **파일**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **방식**: SSH를 통한 원격 stdio 서버 실행
- **상태**: ✅ 설정 완료

### 2. 서버 상태
- **Windows 서버**: 106.241.28.49
- **stdio 서버**: `C:\hwp-mcp\src\hwp_mcp_stdio_server.py`
- **로그**: `C:\hwp-mcp\logs\stdio_stderr.log`
- **상태**: ✅ 정상 작동

### 3. 사용 방법

**Claude Desktop 재시작** 후:

```
HWP 도구가 사용 가능한가요?
```

또는

```
새 HWP 문서를 만들어주세요
```

## 🔑 핵심 교훈

### ❌ 잘못된 접근
- HTTP 서버를 만들어서 stdio를 HTTP로 변환
- 클라이언트 패키지로 HTTP를 다시 stdio로 변환
- 너무 복잡하고 JSON-RPC 형식이 깨짐

### ✅ 올바른 접근
- MCP는 stdio 프로토콜 사용
- Claude Desktop이 직접 stdio 서버와 통신
- 원격 서버는 SSH로 연결

## 📝 변경 사항 요약

### 이전 설정 (작동 안 함)
```json
"hwp-remote": {
  "command": "npx",
  "args": ["-y", "github:swy-datacook/hwp-mcp-client", "http://106.241.28.49:8081"]
}
```

### 새 설정 (작동함)
```json
"hwp-remote": {
  "command": "sshpass",
  "args": [
    "-p", "datacook",
    "ssh", "Data303@106.241.28.49",
    "C:\\hwp-mcp\\venv\\Scripts\\python.exe C:\\hwp-mcp\\src\\hwp_mcp_stdio_server.py 2>C:\\hwp-mcp\\logs\\stdio_stderr.log"
  ]
}
```

## 🚀 다음 단계

1. **Claude Desktop 재시작** (완전히 종료 후 다시 시작)
2. **HWP 도구 확인**
3. **문서 생성 테스트**

## 💡 추가 정보

### 로그 확인
```bash
# 서버 로그 확인
sshpass -p 'datacook' ssh Data303@106.241.28.49 "type C:\\hwp-mcp\\logs\\stdio_stderr.log"
```

### 문제 발생 시
1. SSH 연결 확인: `sshpass -p 'datacook' ssh Data303@106.241.28.49 "echo test"`
2. Python 경로 확인: `C:\hwp-mcp\venv\Scripts\python.exe`
3. 스크립트 경로 확인: `C:\hwp-mcp\src\hwp_mcp_stdio_server.py`

---

**작성일**: 2026-01-31
**상태**: ✅ 해결 완료
